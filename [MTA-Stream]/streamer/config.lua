debug.sethook(nil)
-- Config -- 
USE_LODS = false -- if disabled, we will use the model itself as lod.
FORCE_LODS = true --if enable, the lod will apply to all objects
USE_REQUEST_MODEL = false
-- define map you want load when player join
AUTO_LOAD = {
    --["example"] = true,
}